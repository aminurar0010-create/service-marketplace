# ফেজ ৫ (বাকি অংশ) — ইনভেন্টরি/স্টক ম্যানেজমেন্ট + POS (বাস্তবায়ন সম্পন্ন)

এই দুটো ফিচার একসাথে যোগ করা হয়েছে কারণ এরা একে অপরের সাথে সংযুক্ত —
POS-এ পণ্য বিক্রি হলে ইনভেন্টরি স্টক নিজে থেকেই কমে যায়।

1. **ইনভেন্টরি/স্টক ম্যানেজমেন্ট** ✅
2. **POS (Point of Sale)** ✅

---

## ১. প্রথমে SQL রান করুন (বাধ্যতামূলক)

প্রজেক্টের রুটে থাকা **`supabase_phase5b_inventory_pos_migration.sql`** ফাইলটি
Supabase Dashboard → SQL Editor-এ পেস্ট করে **Run** করুন। এতে তৈরি হবে:

- `inventory_items` টেবিল (পণ্যের ক্যাটালগ: নাম, SKU, ক্যাটাগরি, স্টক পরিমাণ, ক্রয়/বিক্রয়মূল্য)
- `stock_movements` টেবিল (প্রতিটি স্টক ইন/আউট/সমন্বয়ের হিসাব)
- `pos_sales` ও `pos_sale_items` টেবিল (POS বিক্রয় ও তার লাইন আইটেম)
- `create_pos_sale()` ফাংশন — একটি বিক্রয়ে: sale রেকর্ড তৈরি, স্টক কমানো, cash_transactions-এ
  আয় যোগ করা — সবকিছু একটি transaction-এ (আংশিক ব্যর্থতা হবে না)
- `adjust_stock()` ফাংশন — ম্যানুয়াল স্টক ইন/আউট/সমন্বয়ের জন্য
- সবগুলোর RLS পলিসি (শুধু admin/staff দেখতে ও ব্যবহার করতে পারবে)

SQL রান করার আগে সাইট ক্র্যাশ করবে না, তবে নতুন "ইনভেন্টরি" ও "POS" ট্যাব দুটো
ডেটা লোড করতে ব্যর্থ হবে যতক্ষণ না মাইগ্রেশন রান হচ্ছে।

## ২. দুটো নতুন অ্যাডমিন ট্যাব

AdminDashboard-এ এখন দুটো নতুন ট্যাব আছে (রিপোর্টস ও সেটিংস-এর মাঝে):

### ক) ইনভেন্টরি
- পণ্য যোগ/এডিট/ডিলিট, সক্রিয়/নিষ্ক্রিয় টগল
- প্রতিটি পণ্যের নাম, SKU, ক্যাটাগরি, একক (পিস/রিম/প্যাকেট), ক্রয়মূল্য, বিক্রয়মূল্য
- **লো-স্টক সতর্কতা** — কোনো পণ্যের স্টক নির্ধারিত সীমার নিচে নামলে কমলা রঙে হাইলাইট হয়
- **স্টক সমন্বয়** বাটন দিয়ে ম্যানুয়াল স্টক ইন (নতুন ক্রয়), স্টক আউট (নষ্ট/হারানো), বা
  সংশোধন (গণনা ভুল ঠিক করা) করা যায় — প্রতিটির কারণ লেখা যায়
- **স্টক হিস্টোরি** বাটনে প্রতিটি পণ্যের সব পরিবর্তনের লগ দেখা যায়
- উপরে সামারি কার্ড: মোট পণ্য, লো-স্টক সংখ্যা, স্টকের মোট ক্রয়মূল্য

### খ) POS (দ্রুত বিক্রয়)
- দোকানে সরাসরি আসা কাস্টমারের জন্য — কোনো কাস্টমার অ্যাকাউন্ট বা লগইন ছাড়াই দ্রুত বিক্রি করা যায়
- বাম দিকে সার্ভিস ও ইনভেন্টরি পণ্যের তালিকা থেকে ক্লিক করে কার্টে যোগ করা যায়
- ক্যাটালগে নেই এমন কিছু বিক্রি করতে **কাস্টম আইটেম** (নাম + দাম) যোগ করা যায়
- ডান দিকে কার্ট: পরিমাণ বাড়ানো/কমানো, বাদ দেওয়া, কাস্টমারের নাম/ফোন (ঐচ্ছিক),
  পেমেন্ট মেথড (নগদ/বিকাশ/নগদ Nagad/রকেট), ছাড়ের পরিমাণ
- **"বিক্রয় সম্পন্ন করুন"** চাপলে:
  - `pos_sales` ও `pos_sale_items`-এ রেকর্ড তৈরি হয়
  - বিক্রি হওয়া ইনভেন্টরি পণ্যের স্টক স্বয়ংক্রিয়ভাবে কমে যায় (stock_movements-এ লগসহ)
  - ক্যাশ-বুকে (`cash_transactions`) স্বয়ংক্রিয়ভাবে একটি আয়ের এন্ট্রি যোগ হয়
  - একটি প্রিন্টযোগ্য মেমো/রশিদ নতুন উইন্ডোতে খুলে প্রিন্ট ডায়ালগ চালু হয়ে যায়
- নিচে **সাম্প্রতিক বিক্রয়**-এর তালিকা, যেকোনোটির রশিদ যেকোনো সময় পুনরায় প্রিন্ট করা যায়

## ৩. যেসব ফাইল যোগ/পরিবর্তন হয়েছে

- `supabase_phase5b_inventory_pos_migration.sql` (নতুন)
- `src/lib/supabase.ts` — `InventoryItem`, `StockMovement`, `POSSale`, `POSSaleItem` টাইপ যোগ
- `src/lib/receipt.ts` (নতুন) — রশিদ প্রিন্ট ইউটিলিটি
- `src/admin/InventoryTab.tsx`, `InventoryFormModal.tsx`, `StockAdjustModal.tsx` (নতুন)
- `src/admin/POSTab.tsx` (নতুন)
- `src/pages/AdminDashboard.tsx` — নতুন দুটো ট্যাব যোগ করা হয়েছে

## ৪. পরবর্তী ধাপ

ফেজ ৫ এখন সম্পূর্ণ। GitHub-এ কমিট করে Vercel-এ ডিপ্লয় দেখে টেস্ট করুন, তারপর
ফেজ ৬ (প্রিমিয়াম ল্যান্ডিং পেজ) শুরু করা যেতে পারে।
