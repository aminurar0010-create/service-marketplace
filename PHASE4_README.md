# ফেজ ৪ — সিকিউরিটি ও সিস্টেম সেটিংস (বাস্তবায়ন সম্পন্ন)

এই ফেজের সবগুলো ফিচার কোডে যোগ করা হয়েছে:

1. **লাইভ থিম ও কালার কাস্টমাইজেশন প্যানেল** ✅
2. **অ্যাডমিন অ্যাক্টিভিটি লগ** ✅
3. **অটোমেটেড ডাটাবেস ব্যাকআপ** ✅ (ম্যানুয়াল ১-ক্লিক + সার্ভার-সাইড শিডিউলের গাইড)
4. **ডেটা রিটেনশন পলিসি** ✅

---

## ১. প্রথমে SQL রান করুন (বাধ্যতামূলক)

প্রজেক্টের রুটে থাকা **`supabase_phase4_migration.sql`** ফাইলটি Supabase Dashboard →
SQL Editor-এ পেস্ট করে **Run** করুন। এতে তৈরি হবে:

- `site_settings` টেবিল (থিম কালার + রিটেনশন কনফিগ, ১টি রো)
- `activity_logs` টেবিল (অডিট ট্রেইল)
- `export_full_backup()` ফাংশন (অ্যাডমিন-অনলি, পূর্ণ JSON ব্যাকআপ)
- `cleanup_old_data(p_dry_run)` ফাংশন (রিটেনশন পলিসি অনুযায়ী প্রিভিউ/পার্জ)
- সবগুলোর RLS পলিসি

SQL রান করার আগেও সাইট ক্র্যাশ করবে না — থিম প্যানেল ডিফল্ট রঙে চলবে এবং
অ্যাক্টিভিটি লগ/ব্যাকআপ/রিটেনশন প্যানেলে একটি সতর্কবার্তা দেখাবে যে মাইগ্রেশন এখনো রান হয়নি।

## ২. নতুন অ্যাডমিন ট্যাব

ড্যাশবোর্ডে **"সেটিংস"** নামে একটি নতুন ট্যাব যোগ হয়েছে (সবার শেষে), যার ভেতরে
৪টি সাব-ট্যাব আছে:

### ক) থিম কাস্টমাইজেশন
- প্রাইমারি/সেকেন্ডারি/অ্যাকসেন্ট/ব্যাকগ্রাউন্ড — ৪টি কালার পিকার + সাইটের নাম
- রঙ পরিবর্তনের সাথে সাথেই একটি লাইভ প্রিভিউ কার্ড আপডেট হয়
- **"সেভ করুন"** চাপলে পুরো সাইটে (হোমপেজ, নেভবার, বাটন — সবকিছুতে) সাথে সাথে
  প্রয়োগ হয়ে যায়, কোনো রিবিল্ড/রিডিপ্লয় লাগে না
- **কীভাবে কাজ করে:** `tailwind.config.js`-এর ink/seal/brass/paper কালারগুলো
  এখন CSS ভ্যারিয়েবল থেকে আসে (`src/index.css`)। অ্যাডমিন যখন রঙ সেভ করেন,
  `src/lib/theme.ts`-এর `applyTheme()` ফাংশন সেই ভ্যারিয়েবলগুলো
  `document.documentElement`-এ বসিয়ে দেয় (Supabase Realtime সাবস্ক্রিপশনের
  মাধ্যমে অন্য কোনো ব্রাউজারেও সাথে সাথে সিঙ্ক হয়ে যায়)। ফলে কোডের প্রায় সব
  `bg-ink-600`, `text-seal-DEFAULT` ইত্যাদি ক্লাস স্বয়ংক্রিয়ভাবে নতুন রঙ পায়,
  প্রতিটা কম্পোনেন্ট আলাদা করে এডিট করার দরকার হয়নি।

### খ) অ্যাক্টিভিটি লগ
- সাম্প্রতিক ২০০টি অ্যাডমিন/স্টাফ অ্যাকশনের তালিকা (কে, কী করলেন, কখন)
- এন্টিটি টাইপ অনুযায়ী ফিল্টার (order/service/coupon/gallery_photo/profile/theme/system/auth)
- এই মুহূর্তে যেসব অ্যাকশন লগ হয়: লগইন, অর্ডার স্ট্যাটাস/পেমেন্ট/স্টাফ-অ্যাসাইনমেন্ট
  পরিবর্তন, সার্ভিস/কুপন/গ্যালারি তৈরি-এডিট-ডিলিট-টগল, স্টাফ রোল পরিবর্তন ও
  প্রোফাইল আপডেট, থিম পরিবর্তন, ব্যাকআপ ও ডেটা রিটেনশন পার্জ
- নতুন কোনো অ্যাকশন লগ করতে চাইলে যেকোনো জায়গায় শুধু এটুকু কল করলেই হবে:
  ```ts
  import { logActivity } from '../lib/supabase'
  logActivity('অ্যাকশনের বিবরণ', 'entity_type', 'entity_label')
  ```

### গ) ডাটাবেস ব্যাকআপ
- **"এখনই ব্যাকআপ ডাউনলোড করুন"** বাটনে ক্লিক করলে profiles, services, orders,
  coupons, gallery_photos, reviews, cash_transactions ও site_settings —
  সবকিছুর একটি সম্পূর্ণ `.json` ফাইল ডাউনলোড হয়ে যায়
- এটি সম্পূর্ণভাবে কার্যকর, ব্রাউজার থেকেই কাজ করে
- **সত্যিকারের "নিজে নিজে প্রতিদিন" অটোমেশনের জন্য** (ব্রাউজার বন্ধ থাকলেও) সার্ভার-সাইড
  `pg_cron` লাগবে — Panel-এই ধাপে-ধাপে নির্দেশনা দেওয়া আছে, এবং SQL ফাইলের নিচে
  কমেন্ট করা `cron.schedule(...)` কমান্ড আনকমেন্ট করে রান করলেই হবে। Supabase Pro
  প্ল্যানে Point-in-Time Recovery (PITR) থাকলে সেটাই সবচেয়ে শক্তিশালী অটো-ব্যাকআপ।

### ঘ) ডেটা রিটেনশন পলিসি
- সম্পন্ন অর্ডারের ডকুমেন্ট কতদিন পর সাফ হবে, বাতিল অর্ডার কতদিন পর মুছে যাবে,
  আর্থিক আর্কাইভের সীমা — এই তিনটি এখান থেকে সেট করা যায়
- **"প্রিভিউ দেখুন"** — কিছু না মুছেই আগে দেখায় ঠিক কতগুলো রেকর্ড প্রভাবিত হবে
- **"এখনই পার্জ চালান"** — কনফার্মেশনের পর সত্যিকারের ডিলিট/ক্লিনআপ চালায়
  (একই SQL ফাংশন pg_cron দিয়ে প্রতিদিন রাতে অটো-চালানোও যায়, checkbox থেকে এনাবল করুন)

---

## ৩. ডিপ্লয়

স্বাভাবিক নিয়মেই — GitHub-এ কমিট করলে Vercel অটোমেটিক ডিপ্লয় করবে। নতুন কোনো
Environment Variable লাগবে না (আগের VITE_SUPABASE_URL/ANON_KEY-ই যথেষ্ট)।

## ৪. যা এই ফেজে বাদ পড়েছে / সীমাবদ্ধতা (সততার সাথে জানানো হলো)

- সার্ভার বন্ধ অবস্থায় সত্যিকারের প্রতিদিনের অটো-ব্যাকআপ/অটো-পার্জ চালাতে
  Supabase-এর `pg_cron` এক্সটেনশন ম্যানুয়ালি এনাবল করতে হবে (ফ্রন্টএন্ড কোড থেকে
  এটি সম্ভব নয়, এটি Supabase Dashboard থেকেই করতে হয়) — SQL ও নির্দেশনা রেডি করে
  দেওয়া আছে।
- থিম কাস্টমাইজেশন প্রধান ব্র্যান্ড কালার (ink/seal/brass/paper) পরিবর্তন করে, যা
  সাইটের প্রায় সবকিছু কভার করে; তবে কিছু জায়গায় hardcoded রঙ (যেমন charcoal টেক্সট,
  status ব্যাজের yellow/blue/green) থিমযোগ্য নয় — এগুলো ইচ্ছাকৃতভাবে ফিক্সড রাখা
  হয়েছে যাতে স্ট্যাটাস কালারগুলো সবসময় বোধগম্য থাকে।
